# Placeholder

This directory will hold anonymized real projects showing Orion OS in use.
They are the difference between a dead framework and a living one — users
see what a working instance actually looks like, not just empty templates.

Per the v2.0.0 hybrid genericization decision, examples are kept here
(not in the system code) so the system layer stays generic.

Expected contents (subject to revision):
- example-polyrepo (Angular + NestJS, anonymized)
- example-monorepo (React + Supabase, anonymized)
- (more as patterns emerge)

See MIGRATION.md for details on the migration approach.
